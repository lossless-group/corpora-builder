# splash/ — placeholder

This directory reserves the repo's splash site slot per the `maintain-splash-pages`
convention: a small Astro site deployed to GitHub Pages on push to `main`, rendering
this repo's `changelog/` + `context-v/` alongside curated marketing copy.

Scaffold it from the reference implementations (`memopop-site`, `content-farm/splash`,
`lfm/splash`) when the project has something worth showing. Logged as known follow-up
work in `changelog/2026-07-20_01.md` — Phase 1 of this repo ships without a live splash.
